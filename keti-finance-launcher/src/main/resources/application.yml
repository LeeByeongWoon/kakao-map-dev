spring:
  config:
    activate:
      on-profile: "common"
  main:
    web-application-type: none
  pid:
    file: ./keti-finance-launcher.pid

info:
  app:
    name: keti-finance-launcher
    version: 0.0.1-SNAPSHOT

---

spring:
  profiles:
    group:
      "local": "common,local"
      "vm": "common,vm"
      "dev": "common,dev"
      "prod": "common,prod"