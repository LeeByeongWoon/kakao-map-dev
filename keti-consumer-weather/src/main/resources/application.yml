spring:
  config:
    activate:
      on-profile: "common"
  main:
    web-application-type: none
  pid:
    file: ./keti-consumer-weather.pid

info:
  app:
    name: keti-consumer-weather
    version: 0.0.1-SNAPSHOT
    description: kafka consumer

---

spring:
  profiles:
    group:
      "local": "common,local"
      "vm": "common,vm"
      "dev": "common,dev"
      "prod": "common,prod"