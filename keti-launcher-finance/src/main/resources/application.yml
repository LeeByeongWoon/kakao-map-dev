spring:
  config:
    activate:
      on-profile: "common"
  main:
    web-application-type: none
  pid:
    file: ./keti-launcher-finance.pid

info:
  app:
    name: keti-launcher-finance
    version: 0.0.1-SNAPSHOT

---

spring:
  profiles:
    group:
      "local": "common,local"
      "wiz": "common,wiz"
      "dev": "common,dev"
      "prod": "common,prod"